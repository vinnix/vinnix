-----------------------------------------------------------------------------------------
-- Analyzing leafs
-----------------------------------------------------------------------------------------
pkg leaf
      25 dependencies has R-3.5.2                                            and        0 that requirest it.
       2 dependencies has appres-1.0.5                                       and        0 that requirest it.
       8 dependencies has arduino18-1.8.5                                    and        0 that requirest it.
       0 dependencies has arpdig-0.5.6                                       and        0 that requirest it.
       3 dependencies has automake-1.16.1_1                                  and        0 that requirest it.
      23 dependencies has baloo-widgets-18.12.0                              and        0 that requirest it.
       0 dependencies has bdftopcf-1.1_2                                     and        0 that requirest it.
       3 dependencies has bison-3.2.2,1                                      and        0 that requirest it.
       4 dependencies has bitmap-1.0.8_2                                     and        0 that requirest it.
       0 dependencies has boost-jam-1.69.0                                   and        0 that requirest it.
       0 dependencies has bsdadminscripts-6.1.1_8                            and        0 that requirest it.
       4 dependencies has bullet-2.86.1_3                                    and        0 that requirest it.
       3 dependencies has cantarell-fonts-0.101                              and        0 that requirest it.
       1 dependencies has check-0.12.0_1                                     and        0 that requirest it.
       6 dependencies has cmake-3.13.2                                       and        0 that requirest it.
       0 dependencies has cuse4bsd-kmod-0.1.36                               and        0 that requirest it.
       0 dependencies has dialog4ports-0.1.6                                 and        0 that requirest it.
       5 dependencies has doxygen-1.8.14_1,2                                 and        0 that requirest it.
       1 dependencies has drm-next-kmod-4.11.g20181027_1                     and        0 that requirest it.
       2 dependencies has eclipse-cdt-9.0.1                                  and        0 that requirest it.
       2 dependencies has efivar-0.15_6                                      and        0 that requirest it.
       0 dependencies has eigen-3.3.5                                        and        0 that requirest it.
       3 dependencies has elasticsearch6-6.4.2_1                             and        0 that requirest it.
       3 dependencies has emojione-color-font-ttf-1.4_1                      and        0 that requirest it.
       1 dependencies has exempi-2.2.2_1                                     and        0 that requirest it.
       0 dependencies has fftw3-float-3.3.8_3                                and        0 that requirest it.
      41 dependencies has firefox-64.0_3,1                                   and        0 that requirest it.
       9 dependencies has fltk-1.3.4                                         and        0 that requirest it.
      11 dependencies has fluxbox-1.3.7_4                                    and        0 that requirest it.
       1 dependencies has fpc-opengl-3.0.4                                   and        0 that requirest it.
      11 dependencies has fpc-x11-3.0.4_2                                    and        0 that requirest it.
       0 dependencies has freetype-1.3.1_5                                   and        0 that requirest it.
       1 dependencies has frei0r-1.6.1                                       and        0 that requirest it.
       2 dependencies has fusefs-lkl-4.16.g20180628_2                        and        0 that requirest it.
       3 dependencies has fusefs-ntfs-2017.3.23                              and        0 that requirest it.
       1 dependencies has gcc-8_3                                            and        0 that requirest it.
       6 dependencies has gcc6-6.5.0_3                                       and        0 that requirest it.
      11 dependencies has gconfmm-2.28.2_7                                   and        0 that requirest it.
       2 dependencies has gdisk-1.0.4_3                                      and        0 that requirest it.
       2 dependencies has gettext-0.19.8.1                                   and        0 that requirest it.
      11 dependencies has git-2.20.1                                         and        0 that requirest it.
       1 dependencies has gnugo-3.8_1                                        and        0 that requirest it.
       0 dependencies has go-1.11.4,1                                        and        0 that requirest it.
       2 dependencies has gpac-libgpac-0.5.0_2,1                             and        0 that requirest it.
       8 dependencies has groff-1.22.3_1                                     and        0 that requirest it.
       3 dependencies has grub2-2.00_13                                      and        0 that requirest it.
       0 dependencies has gsfonts-8.11_8                                     and        0 that requirest it.
       7 dependencies has gstreamer-plugins-0.10.36_11,3                     and        0 that requirest it.
       5 dependencies has gtk-engines2-2.20.2_3                              and        0 that requirest it.
       0 dependencies has hcidump-1.5.1                                      and        0 that requirest it.
       1 dependencies has i386-wine-devel-4.0.r1_1,1                         and        0 that requirest it.
       2 dependencies has intltool-0.51.0_1                                  and        0 that requirest it.
       1 dependencies has ipmitool-1.8.18_1                                  and        0 that requirest it.
       5 dependencies has ja-tex-ptex-3.6_1                                  and        0 that requirest it.
      46 dependencies has kate-18.12.0                                       and        0 that requirest it.
       2 dependencies has kdeadmin-18.12.0                                   and        0 that requirest it.
      18 dependencies has kdeedu-18.12.0                                     and        0 that requirest it.
      28 dependencies has kdegames-18.12.0                                   and        0 that requirest it.
      10 dependencies has kdegraphics-18.12.0                                and        0 that requirest it.
       4 dependencies has kdemultimedia-18.12.0                              and        0 that requirest it.
       3 dependencies has kdenetwork-18.12.0                                 and        0 that requirest it.
      51 dependencies has kdepim-18.08.3                                     and        0 that requirest it.
      12 dependencies has kdeutils-18.12.0_1                                 and        0 that requirest it.
      36 dependencies has kdialog-18.12.0                                    and        0 that requirest it.
      39 dependencies has khelpcenter-18.12.0                                and        0 that requirest it.
       1 dependencies has kibana6-6.4.2                                      and        0 that requirest it.
      29 dependencies has kigo-18.12.0                                       and        0 that requirest it.
      54 dependencies has konqueror-18.12.0                                  and        0 that requirest it.
      44 dependencies has konsole-18.12.0                                    and        0 that requirest it.
       0 dependencies has libargon2-20171227_1                               and        0 that requirest it.
       1 dependencies has libatomic_ops-7.6.4                                and        0 that requirest it.
       1 dependencies has libchk-1.10.3                                      and        0 that requirest it.
       0 dependencies has libfpx-1.3.1.10                                    and        0 that requirest it.
      10 dependencies has libglademm-2.6.7_11                                and        0 that requirest it.
       0 dependencies has libijs-0.35_5                                      and        0 that requirest it.
       0 dependencies has libiodbc-3.52.12                                   and        0 that requirest it.
       2 dependencies has libmng-1.0.10_3                                    and        0 that requirest it.
       1 dependencies has libmtp-1.1.16                                      and        0 that requirest it.
       0 dependencies has libsodium-1.0.16                                   and        0 that requirest it.
       0 dependencies has libsoxr-0.1.2.20160529_9                           and        0 that requirest it.
       0 dependencies has libsrtp-1.6.0_2                                    and        0 that requirest it.
       0 dependencies has libssh-0.8.4                                       and        0 that requirest it.
       5 dependencies has libstreamanalyzer-0.7.8_13                         and        0 that requirest it.
       1 dependencies has libsynaptics-0.14.6.c                              and        0 that requirest it.
       2 dependencies has libtool-2.4.6_1                                    and        0 that requirest it.
      13 dependencies has libwraster-0.95.8_1                                and        0 that requirest it.
       0 dependencies has libyubikey-1.13                                    and        0 that requirest it.
       0 dependencies has linuxfdisk-2.11z                                   and        0 that requirest it.
       1 dependencies has lua52-bitop-1.0.2_1                                and        0 that requirest it.
       2 dependencies has lua52-json-1.3.4                                   and        0 that requirest it.
       2 dependencies has lua52-luaexpat-1.3.0_4                             and        0 that requirest it.
       1 dependencies has lua52-luasocket-3.0.r1_3,1                         and        0 that requirest it.
       0 dependencies has luajit-2.0.5_3                                     and        0 that requirest it.
       2 dependencies has luit-1.1.1_1                                       and        0 that requirest it.
       0 dependencies has miniupnpc-2.1_1                                    and        0 that requirest it.
       0 dependencies has mixer-1.0                                          and        0 that requirest it.
      12 dependencies has mutt-1.11.1                                        and        0 that requirest it.
       0 dependencies has nasm-2.13.03,1                                     and        0 that requirest it.
       1 dependencies has ninja-1.8.2,2                                      and        0 that requirest it.
      10 dependencies has nspluginwrapper-1.4.4_7                            and        0 that requirest it.
      59 dependencies has okular-18.12.0_1                                   and        0 that requirest it.
       0 dependencies has opengl-man-657.2                                   and        0 that requirest it.
       0 dependencies has openslp-2.0.0_1                                    and        0 that requirest it.
       0 dependencies has openssl111-1.1.1a                                  and        0 that requirest it.
       1 dependencies has p5-IO-Socket-IP-0.39                               and        0 that requirest it.
       2 dependencies has p5-Locale-gettext-1.07                             and        0 that requirest it.
       1 dependencies has p5-MIME-Base64-3.15                                and        0 that requirest it.
       1 dependencies has p5-Socket-2.027                                    and        0 that requirest it.
       0 dependencies has pam_kde-1.0                                        and        0 that requirest it.
       2 dependencies has pciutils-3.6.2                                     and        0 that requirest it.
       0 dependencies has pftop-0.7_8                                        and        0 that requirest it.
       0 dependencies has pkg-1.10.5_5                                       and        0 that requirest it.
      34 dependencies has plasma5-plasma-5.12.7                              and        0 that requirest it.
      15 dependencies has poppler-glib-0.72.0                                and        0 that requirest it.
       3 dependencies has postgresql11-contrib-11.1                          and        0 that requirest it.
       0 dependencies has postgresql11-docs-11.1_2                           and        0 that requirest it.
       2 dependencies has postgresql11-plpython-11.1_1                       and        0 that requirest it.
       1 dependencies has pt_BR-libreoffice-6.0.7                            and        0 that requirest it.
       0 dependencies has pth-2.0.7_1                                        and        0 that requirest it.
       3 dependencies has py27-dateutil-2.7.3                                and        0 that requirest it.
       4 dependencies has py27-dbus-1.2.8                                    and        0 that requirest it.
      10 dependencies has py27-gtk2-2.24.0_5                                 and        0 that requirest it.
       2 dependencies has py27-nose-1.3.7                                    and        0 that requirest it.
       7 dependencies has py27-numpy-1.15.4_1,1                              and        0 that requirest it.
       6 dependencies has py27-rdflib-4.2.2                                  and        0 that requirest it.
       1 dependencies has py27-sip-4.19.13_1,1                               and        0 that requirest it.
      15 dependencies has py27-sphinx-1.6.5_1,1                              and        0 that requirest it.
       2 dependencies has py36-virtualenv-16.0.0                             and        0 that requirest it.
       4 dependencies has pycharm-ce-2018.3.2                                and        0 that requirest it.
       2 dependencies has python-2.7_3,2                                     and        0 that requirest it.
       1 dependencies has qjson-qt5-0.9.0_3                                  and        0 that requirest it.
       2 dependencies has qt5-buildtools-5.12.0_1                            and        0 that requirest it.
       1 dependencies has qt5-qmake-5.12.0_1                                 and        0 that requirest it.
       0 dependencies has radeontool-1.5                                     and        0 that requirest it.
       3 dependencies has rkhunter-1.4.4                                     and        0 that requirest it.
       1 dependencies has rsync-3.1.3                                        and        0 that requirest it.
       1 dependencies has schroedinger-1.0.11_4                              and        0 that requirest it.
       3 dependencies has scons-3.0.1                                        and        0 that requirest it.
       1 dependencies has screen-4.6.2_1                                     and        0 that requirest it.
      10 dependencies has sdl2-2.0.9_2                                       and        0 that requirest it.
       6 dependencies has sdl_mixer-1.2.12_12                                and        0 that requirest it.
       1 dependencies has sessreg-1.1.1                                      and        0 that requirest it.
       2 dependencies has setxkbmap-1.3.1                                    and        0 that requirest it.
       0 dependencies has shared-desktop-ontologies-0.11.0                   and        0 that requirest it.
       1 dependencies has silgraphite-2.3.1_4                                and        0 that requirest it.
       2 dependencies has smproxy-1.0.6                                      and        0 that requirest it.
       0 dependencies has soundtouch-1.9.2_6                                 and        0 that requirest it.
       1 dependencies has ssd_report-smartmontools-0.2                       and        0 that requirest it.
       7 dependencies has subversion-1.11.0_1                                and        0 that requirest it.
       1 dependencies has sudo-1.8.26                                        and        0 that requirest it.
       0 dependencies has svgalib-1.4.3_7                                    and        0 that requirest it.
       8 dependencies has tex-aleph-1.15.2.1.r.4_4                           and        0 that requirest it.
       3 dependencies has tex-jadetex-3.13_3                                 and        0 that requirest it.
      14 dependencies has tex-xdvik-22.87_4                                  and        0 that requirest it.
      14 dependencies has tex-xetex-0.99992_21                               and        0 that requirest it.
       3 dependencies has texi2html-5.0_2,1                                  and        0 that requirest it.
       1 dependencies has tmake-2.12                                         and        0 that requirest it.
       6 dependencies has twm-1.0.10                                         and        0 that requirest it.
       6 dependencies has unique-1.1.6_7                                     and        0 that requirest it.
       4 dependencies has vala-0.40.11,1                                     and        0 that requirest it.
      18 dependencies has vim-8.1.0555                                       and        0 that requirest it.
       1 dependencies has virtual_oss-1.2.0                                  and        0 that requirest it.
      30 dependencies has virtualbox-ose-5.2.22_2                            and        0 that requirest it.
       9 dependencies has virtualbox-ose-additions-5.2.22_2                  and        0 that requirest it.
      48 dependencies has vlc-2.2.8_16,4                                     and        0 that requirest it.
       0 dependencies has wallpapers-freebsd-kde4-1.0_1                      and        0 that requirest it.
       4 dependencies has wget-1.20                                          and        0 that requirest it.
       0 dependencies has wmicons-1.0_2                                      and        0 that requirest it.
       5 dependencies has x11perf-1.6.0                                      and        0 that requirest it.
       4 dependencies has x264-0.155.2917_5                                  and        0 that requirest it.
       4 dependencies has xauth-1.0.10                                       and        0 that requirest it.
       5 dependencies has xbacklight-1.2.2                                   and        0 that requirest it.
       6 dependencies has xcalc-1.0.6_2                                      and        0 that requirest it.
       1 dependencies has xcb-proto-1.13                                     and        0 that requirest it.
       5 dependencies has xclock-1.0.7_2                                     and        0 that requirest it.
       1 dependencies has xcmsdb-1.0.5                                       and        0 that requirest it.
       4 dependencies has xconsole-1.0.7_1                                   and        0 that requirest it.
       0 dependencies has xcursor-themes-1.0.5_2                             and        0 that requirest it.
       3 dependencies has xcursorgen-1.0.6_3                                 and        0 that requirest it.
      11 dependencies has xdpyinfo-1.3.2_2                                   and        0 that requirest it.
       2 dependencies has xev-1.2.2                                          and        0 that requirest it.
       2 dependencies has xf86-input-keyboard-1.9.0_3                        and        0 that requirest it.
       2 dependencies has xf86-input-mouse-1.9.3_2                           and        0 that requirest it.
       5 dependencies has xf86-video-ati-18.1.0,1                            and        0 that requirest it.
       5 dependencies has xf86dga-1.0.3_1                                    and        0 that requirest it.
       2 dependencies has xfsprogs-3.2.4_1                                   and        0 that requirest it.
       2 dependencies has xgamma-1.0.6                                       and        0 that requirest it.
       2 dependencies has xgc-1.0.5                                          and        0 that requirest it.
       3 dependencies has xhost-1.0.7                                        and        0 that requirest it.
       1 dependencies has xinit-1.4.0,1                                      and        0 that requirest it.
       5 dependencies has xinput-1.6.2_2                                     and        0 that requirest it.
       2 dependencies has xkbevd-1.1.4                                       and        0 that requirest it.
       3 dependencies has xkbutils-1.0.4_2                                   and        0 that requirest it.
       2 dependencies has xkill-1.0.5                                        and        0 that requirest it.
       2 dependencies has xlsatoms-1.1.2                                     and        0 that requirest it.
       3 dependencies has xlsclients-1.1.4                                   and        0 that requirest it.
       8 dependencies has xmlto-0.0.28                                       and        0 that requirest it.
       1 dependencies has xmodmap-1.0.9                                      and        0 that requirest it.
       0 dependencies has xorg-docs-1.7.1,1                                  and        0 that requirest it.
       8 dependencies has xorg-fonts-7.7_1                                   and        0 that requirest it.
      40 dependencies has xorg-libraries-7.7_2                               and        0 that requirest it.
       0 dependencies has xorg-macros-1.19.2                                 and        0 that requirest it.
       2 dependencies has xpr-1.0.5                                          and        0 that requirest it.
       2 dependencies has xrdb-1.1.1                                         and        0 that requirest it.
       1 dependencies has xrefresh-1.0.6                                     and        0 that requirest it.
       2 dependencies has xsetmode-1.0.0                                     and        0 that requirest it.
       9 dependencies has xterm-340                                          and        0 that requirest it.
       2 dependencies has xvinfo-1.1.3                                       and        0 that requirest it.
       2 dependencies has xwd-1.0.7                                          and        0 that requirest it.
       2 dependencies has xwininfo-1.1.4_2                                   and        0 that requirest it.
       1 dependencies has xwud-1.0.5                                         and        0 that requirest it.
       1 dependencies has yasm-1.3.0                                         and        0 that requirest it.
